# Every path in this repo is owned by @TadMSTR, who is automatically requested for review on
# any pull request.
#
# This is also an OSSF Scorecard check (Code-Review): Scorecard reads a present CODEOWNERS as
# evidence that changes have a defined reviewer, so the single line below does more work than
# it looks like.
* @TadMSTR
